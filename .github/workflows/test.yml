# github workflow
name: Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v3
      - name: install dependencies
        run: npm install
      - name: reason
        run: echo "test are failing because db is not set up"
      # - name: run tests
      #   run: npm test
